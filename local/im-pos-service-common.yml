server:
  port: 8187
spring:
  application:
    name: im-pos-service-common    # 项目名称尽量用小写
  jmx:
    default-domain: im-pos-service-common
impos:
  location: /Users/elnino/Workspace/runtime/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8180/eureka/    # 指定注册中心的地址
  instance:
      preferIpAddress: true
